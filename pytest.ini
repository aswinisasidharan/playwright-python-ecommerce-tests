[pytest]
markers =
    smoke: quick, high-priority checks - run first, on every push
    sanity: narrow tests verifying specific functionality after changes
    regression: full coverage, including edge-case and quirky-user scenarios
    login: tests related to login functionality
    checkout: tests related to checkout functionality
    cart: tests related to cart functionality
addopts = --strict-markers
